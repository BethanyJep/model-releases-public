# 00 — Setup

> Model: {{display_name}} (`{{slug}}`)

## Goal

Get the deployment, credentials, and tooling in place so the rest of the
exploration runs cleanly. The `explore-model/setup` subskill will walk
you through this file one check at a time.

## Prereqs

- TODO — Microsoft Foundry project with permission to deploy or call
  `{{deployment_name}}`.
- TODO — local tools (e.g. `az`, language SDKs, sample environment).
- TODO — permissions / roles.

## Steps

1. TODO — confirm or create the Foundry project for this model.
2. TODO — confirm `{{deployment_name}}` is deployed (or deploy it).
3. TODO — sign in and select the subscription / project locally.
4. TODO — set required environment variables (endpoint, deployment name).

## Verify

- TODO — a one-line probe that proves the model is callable
  (e.g. a tiny `chat.completions` or `embeddings` request).

## Troubleshoot

| Symptom | Likely cause | Fix |
|---|---|---|
| TODO | TODO | TODO |

## Next

Move to `01-…md`.

{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://github.com/microsoft-foundry/model-releases/schemas/publisher.schema.json",
  "title": "Publisher",
  "description": "Frontmatter contract for a publisher README.",
  "type": "object",
  "additionalProperties": false,
  "required": ["kind", "name", "slug", "one_line"],
  "properties": {
    "kind": { "const": "publisher" },
    "name": {
      "type": "string",
      "description": "Human-readable publisher name, matching the catalog's publisher facet where one exists (e.g. 'Azure OpenAI')."
    },
    "slug": {
      "type": "string",
      "pattern": "^[a-z0-9]+(-[a-z0-9]+)*$",
      "description": "Kebab-case folder slug under models/."
    },
    "one_line": {
      "type": "string",
      "description": "Short pitch used in the repo README publisher table."
    },
    "provider": {
      "type": ["string", "null"],
      "description": "Underlying provider or vendor."
    },
    "related_primers": {
      "type": "array",
      "items": { "type": "string" },
      "description": "Slugs of primers most relevant to this publisher (docs/primers/<slug>.md)."
    }
  }
}
